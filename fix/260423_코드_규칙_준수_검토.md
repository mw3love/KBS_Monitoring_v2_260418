# 코드 규칙 준수 검토 보고서 — 260423

> 대상: 전체 코드베이스 (`processes/`, `detection/`, `ui/`, `ipc/`, `utils/`, `config/`)
> 범위: CLAUDE.md / docs_ipc_spec.md / docs_기능_레퍼런스.md 규칙 대비 코드 준수 여부
> 정답 기준: 규칙 위반 항목은 규칙에 맞게 코드 수정. 규칙 부재 항목은 사용자 판단.
> 감사 방법: Explore 에이전트 3개 병렬 실행 (카테고리 A+B, C, D) + 수동 검증

---

## 요약

| # | 심각도 | 항목 | 위치 |
|---|---|---|---|
| **#1** | **Critical** | C-6: 스틸 감지 파라미터 전달 실패 (Plan #1 이월) | `processes/detection_process.py:96` |
| #2 | Major | CLAUDE.md 200줄 분리 규칙 위반 (344줄) | `CLAUDE.md` |
| #3 | Major | DetectionCrashed 재시도 부족 (drop 금지 위반) | `processes/watchdog_process.py:200-231` |
| #4 | Minor | `requirements.txt` 미존재 (Plan #1 이월) | 루트 |
| #5 | Minor | `audio_tone_*` 미사용 키 — 주석 없음 (Plan #1 이월) | `utils/config_manager.py:36-38` |
| #6 | Minor | DIAG try-except가 `pass`로 에러 삼킴 | `processes/detection_process.py:716-806` |

**OK 확인 (~25건)**: Detection PySide6 금지, 메인 루프 패턴, `freeze_support`/`faulthandler`, `read_frame().copy()`, SharedMemory 잔존정리, 히스테리시스 타이머 초기화, 정파 억제 그룹별(`is_signoff_label`), AlarmSystem UI 전용, 텔레그램 주체 분리, 로그 파일 분리, 설정 3파일 동기화, QSpinBox 미사용, 색상 원칙, 7탭 설정, 비모달 다이얼로그(`main_window.py:359 show()`), IPC 메시지 23개 완전성, SharedMemory 레이아웃, Lamport seq_no, Queue maxsize(200/50), put_nowait 1-drop 패턴, 기동/정상종료/재spawn 시퀀스, heartbeat(5s 쓰기/10s 감시), cmd_event 경로, DIAG 7개 섹션 독립 try-except.

---

## A. 아키텍처 원칙 감사

### 발견 없음

- Detection PySide6 임포트 금지: **OK** (`processes/detection_process.py` 전수 확인)
- `freeze_support`/`faulthandler`: **OK** (`main.py` 최상단 확인)
- Watchdog ↔ Detection 소유권 분리: **OK** (Watchdog만 spawn/kill)
- `shutdown_event` 의도적 종료 플래그: **OK** (`watchdog_process.py:127`)
- heartbeat 5초 쓰기/10초 감시: **OK** (`HeartbeatWriter.run()`, `_HB_STALE_SEC=10.0`)

---

## B. IPC 스펙 감사

### 발견: #3 DetectionCrashed drop 금지 위반 *(Major)*

| 항목 | 내용 |
|---|---|
| **스펙 위치** | `docs_ipc_spec.md §2.3` — DetectionCrashed는 drop 금지 목록 |
| **코드 위치** | `processes/watchdog_process.py:200-205, 222-231` |
| **문제** | `put_nowait()` 직호출 + `except Exception: pass` → Queue Full 시 조용히 소실 |
| **해결** | `_put_nodrop` 3회 재시도 패턴 적용 (detection_process.py:69-82 동일 패턴) |

추가: `process_dead` 경로(200-205)에서 `stale_sec` 인자 누락 → 명시적으로 `stale_sec=0.0` 전달.

### OK 확인

- IPC 메시지 23개 완전성: **OK** (`ipc/messages.py` 전수 확인)
- SharedMemory 레이아웃 (`kbs_frame_v2`/`kbs_state_v2`): **OK**
- Lamport seq_no 기반 변경 감지: **OK**
- Queue maxsize (result=200, cmd=50): **OK**
- put_nowait 1-drop 패턴: **OK** (detection_process.py `_put()` 함수)
- cmd_event 경로: **OK** (cmd_queue polling + cmd_event signal 병행)

---

## C. 기능 구현 감사

### 발견: #1 C-6 스틸 감지 파라미터 전달 실패 *(Critical)*

| 항목 | 내용 |
|---|---|
| **설정 키** | `config/default_config.json:11`, `utils/config_manager.py:20`: `"still_changed_ratio": 10.0` |
| **UI 저장** | `ui/settings_dialog.py:732, 1296, 1526`: 모두 `still_changed_ratio` 키로 저장/로드 |
| **Detector 속성** | `detection/detector.py:37`: `self.still_block_threshold = 10.0` |
| **Detector 사용** | `detection/detector.py:78`: `threshold = self.still_block_threshold` |
| **전달 코드** | `processes/detection_process.py:96`: `detector.still_threshold = det.get("still_threshold", 8)` — **유령 키** |
| **결과** | 사용자가 설정을 변경해도 Detector 임계값은 초기값 10.0에 고정됨 |
| **해결 (B안)** | `detector.py`의 `still_block_threshold` → `still_changed_ratio` 리네이밍 + `detection_process.py:96` 수정 |

### OK 확인

- 히스테리시스 타이머 초기화: **OK** (`signoff_manager.py` 확인)
- 정파 억제 그룹별 `is_signoff_label()`: **OK**
- `is_any_signoff()` 미사용: **OK**
- PREPARATION 상태 블랙 계속 알림: **OK**
- AlarmSystem UI 전용: **OK** (`ui/alarm.py`)
- 텔레그램 주체 분리 ([DETECT]/[SYSTEM]): **OK**
- 7탭 설정 다이얼로그: **OK**
- 비모달 다이얼로그 `show()`: **OK** (`main_window.py:359`)
- 자동 녹화 순환버퍼: **OK** (`auto_recorder.py`)

---

## D. 코드 품질 감사

### 발견: #2 CLAUDE.md 200줄 분리 규칙 위반 *(Major)*

| 항목 | 내용 |
|---|---|
| **규칙** | `CLAUDE.md §CLAUDE.md 분리 규칙`: 200줄 초과 시 하위 폴더 CLAUDE.md 생성 |
| **실제** | `CLAUDE.md` 344줄, 하위 CLAUDE.md 0개 |
| **해결** | `detection/CLAUDE.md`, `ui/CLAUDE.md` 생성 후 관련 섹션 이동 |

### 발견: #5 audio_tone_* 미사용 키 *(Minor)*

| 항목 | 내용 |
|---|---|
| **위치** | `utils/config_manager.py:36-38`, `config/default_config.json:27-29` |
| **키** | `audio_tone_std_threshold`, `audio_tone_duration`, `audio_tone_min_level` |
| **사용처** | detection/*.py · processes/*.py · ui/*.py 전수 grep → **사용처 없음** |
| **해결** | `config_manager.py` 해당 블록에 미사용 주석 추가 |

### 발견: #6 DIAG try-except 에러 삼킴 *(Minor)*

| 항목 | 내용 |
|---|---|
| **위치** | `processes/detection_process.py:716-806` |
| **문제** | 7개 DIAG 섹션 각각 `except Exception: pass` → 실패 진단 불가 |
| **규칙** | CLAUDE.md §예외 처리 원칙: 에러 로그 필수 |
| **해결** | 모듈 레벨 `_log = logging.getLogger(__name__)` 추가, 각 except에 `_log.error(...)` 1줄 추가 |

### 발견: #4 requirements.txt 미존재 *(Minor)*

| 항목 | 내용 |
|---|---|
| **규칙** | `CLAUDE.md §설치된 외부 패키지` (8개 패키지 명시) |
| **실제** | 루트에 `requirements.txt` 없음 |
| **해결** | CLAUDE.md §설치된 외부 패키지 기준으로 생성 |

### OK 확인

- 로그 파일 분리 (detection/ui/watchdog 각각): **OK**
- 설정 3파일 동기화 (`default_config.json` / `config_manager.py` / `kbs_config.json`): **OK**
- QSpinBox 위아래 버튼: **OK** (미사용 확인)
- 색상 원칙 (정상=무색, 이상=빨간색): **OK**
- `read_frame().copy()` 규칙: **OK** (`ipc/shared_frame.py`)
- SharedMemory 잔존 정리: **OK** (`main.py` 시작/종료 시)
- 예외 처리 독립 try-except (메인 루프): **OK**
- DIAG 7개 섹션 독립 try-except 구조: **OK** (에러 로그 누락만 문제)

---

## Plan #1 이월 항목

| Plan #1 이월 # | 항목 | 이번 Plan #2 처리 |
|---|---|---|
| A-1 | `requirements.txt` 미존재 | #4 수정 완료 (생성) |
| C-6 | 스틸 감지 파라미터 전달 실패 | #1 수정 완료 (B안 리네이밍) |
| C-7 | `audio_tone_*` 미사용 키 | #5 수정 완료 (주석 추가) |

---

## 승인 결과

사용자 승인 완료 (2026-04-23):

1. **#1 C-6 해결**: **(B) 전체 리네이밍** — `detector.py`의 `still_block_threshold` → `still_changed_ratio`
2. **#2 CLAUDE.md 분리**: **detection/ui 2개** 생성
3. **#6 DIAG 로그**: **전체 7개** 섹션 모두 로그 추가

---

## 반영 완료 (커밋 대상 목록)

> 아래 섹션은 Step 4에서 수정 완료 후 추가됨.

### 수정 완료 파일

| 파일 | 이슈 | 변경 내용 |
|---|---|---|
| `detection/detector.py` | #1 (B) | `still_block_threshold` → `still_changed_ratio` 리네이밍 (2곳) |
| `processes/detection_process.py` | #1 (B), #6 | `still_threshold` → `still_changed_ratio` 수정 + 모듈 `_log` 추가 + DIAG 7개 except 로그 |
| `processes/watchdog_process.py` | #3 | `_nodrop_put` 헬퍼 추가, DetectionCrashed 2곳에 재시도 패턴 적용 |
| `CLAUDE.md` | #2 | detection/ui 관련 섹션 이동 후 참조 추가 (~230줄로 축소) |
| `detection/CLAUDE.md` | #2 | 신규 생성: Detection 전용 규칙 |
| `ui/CLAUDE.md` | #2 | 신규 생성: UI 전용 규칙 |
| `requirements.txt` | #4 | 신규 생성: 8개 패키지 |
| `utils/config_manager.py` | #5 | `audio_tone_*` 블록에 미사용 주석 추가 |
| `fix/260423_코드_규칙_준수_검토.md` | — | 본 보고서 |
