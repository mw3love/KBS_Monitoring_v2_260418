# 문서 드리프트 보고서 — 260423

> 대상: Core 4 문서 (`CLAUDE.md`, `PROGRESS.md`, `docs_ipc_spec.md`, `docs_기능_레퍼런스.md`)
> 범위: **사실 기술 오류**만 수집. 규칙 준수·코드 품질 검토는 Plan #2로 이월.
> 정답 기준: 명백한 불일치는 **코드를 정답**으로 두고 문서를 갱신. 의도가 애매한 항목은 사용자 판단.
> 재현 기준: 각 행에 문서 위치(파일·섹션)와 코드 위치(파일·라인)를 명시.

---

## 요약

| 카테고리 | 명백 | 애매 | 합계 |
|---|---:|---:|---:|
| A. 파일·패키지·외부도구 | 0 | 1 | 1 |
| B. IPC 스펙 (`docs_ipc_spec.md`) | 4 | 0 | 4 |
| C. 기능 레퍼런스 (`docs_기능_레퍼런스.md`) | 5 | 2 | 7 |
| D. 진행표 (`PROGRESS.md`) | 1 | 0 | 1 |
| E. 루트 지침 (`CLAUDE.md`) | 0 | 0 | 0 |
| **합계** | **10** | **3** | **13** |

---

## A. 파일·패키지·외부도구

### A-1. `requirements.txt` 부재  *(애매 — 문서 드리프트가 아닌 아티팩트 결함)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | `CLAUDE.md` §설치된 외부 패키지 (코드 블록 형태로 8개 패키지 나열) |
| **문서 내용** | PySide6, opencv-python, numpy, sounddevice, psutil, gputil, pycaw, requests 명시 |
| **실제 상태** | 루트에 `requirements.txt` 파일 **없음**. 각 프로세스 import로만 확인 가능 |
| **제안 수정** | 보고서에 상태만 기록. 실제 `requirements.txt` 생성 여부는 별도 결정 (Plan #1 비범위) |
| **태그** | 애매 (생성 vs. 현 상태 유지 — 사용자 판단) |

**기타(문서 드리프트 아님 — 참고용)**: `CLAUDE.md §파일 구조` 트리의 주요 디렉터리(`processes/`, `ipc/`, `detection/`, `ui/`, `core/`, `utils/`, `config/`, `resources/`, `data/`)는 모두 존재. 불일치 없음.

---

## B. IPC 스펙 (`docs_ipc_spec.md`)

### B-1. `cmd_event` 누락  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §0 프로세스 토폴로지 (12~22행) |
| **문서 내용** | `shutdown_event` 만 "정상 종료 브로드캐스트"로 기재. `cmd_event`는 언급 없음 |
| **실제 코드** | `main.py:97` — `cmd_event = multiprocessing.Event()` 생성 → Watchdog(`main.py:108`)·Detection(`watchdog_process.py:140`)에 전달. 용도: cmd_queue 도착 즉시 깨우기 (Detection `cmd_event.wait()` 사용) |
| **제안 수정** | §0 토폴로지에 `multiprocessing.Event cmd_event [cmd_queue 도착 알림, Detection 폴링 지연 제거]` 행 추가. §3.1 기동 시퀀스 4단계에도 "`cmd_event` 생성" 포함 |
| **태그** | 명백 |

### B-2. `DetectionCrashed` 메시지 누락  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §2.1 result_queue 메시지 표 (85~97행) — 11개 메시지 나열 |
| **문서 내용** | DetectionResult / AlarmTrigger / AlarmResolve / LogEntry / DiagSnapshot / SignoffStateChange / RecordingEvent / TelegramStatus / StreamError / DetectionReady / PerfMeasurement (11개) |
| **실제 코드** | `ipc/messages.py:97-103` — `DetectionCrashed(dead_pid:int, reason:str('process_dead'\|'heartbeat_stale'), stale_sec:float)` 존재. `RESULT_MESSAGES` 튜플(라인 179~183)에도 포함 → **총 12개**. `watchdog_process.py:201-204, 224-229`에서 발행 |
| **제안 수정** | §2.1 표에 행 추가: `DetectionCrashed \| dead_pid:int, reason:str('process_dead'\|'heartbeat_stale'), stale_sec:float \| Watchdog이 Detection 비정상 종료 감지 시 재spawn 직전 발행 (drop 금지)` |
| **태그** | 명백 |

### B-3. 정상 종료 시 Watchdog join 타임아웃 불일치  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §3.2 정상 종료 시퀀스 7단계 (150행) |
| **문서 내용** | `main`: Watchdog join(timeout=**5s**) → 실패 시 terminate |
| **실제 코드** | `main.py:240` — `watchdog_proc.join(timeout=**8.0**)` |
| **제안 수정** | 문서를 `timeout=8s`로 갱신 (코드가 정답) |
| **태그** | 명백 |

### B-4. 재spawn 시 Watchdog이 발행하는 알림 메시지 불일치  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §3.3 Detection 재spawn 복원 시퀀스 2단계 (158행) |
| **문서 내용** | `result_queue.put(LogEntry(level='error', ...))` |
| **실제 코드** | `watchdog_process.py:201-204` (process_dead 경로), `watchdog_process.py:224-229` (heartbeat_stale 경로) — 모두 `DetectionCrashed(dead_pid, reason, stale_sec)` 발행. LogEntry 아님 |
| **제안 수정** | 문서를 `result_queue.put(DetectionCrashed(dead_pid, reason, stale_sec))` 로 갱신 (B-2와 함께 반영) |
| **태그** | 명백 |

---

## C. 기능 레퍼런스 (`docs_기능_레퍼런스.md`)

### C-1. 탭 6 예약 재시작 UI 설명이 구 스키마(2-슬롯)  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §2.5 탭 6 알림설정 (164행) |
| **문서 내용** | "**자동 재시작** 섹션: 재시작 시각 1/2 (HH:MM, 각 활성화 체크박스), OS 리소스 초기화 목적" |
| **실제 코드** | `main.py:184-222` — "기준 시각 + 주기(시간) + 제외 시간대" 방식으로 완전 개편. `default_config.json:107-110` — `scheduled_restart_base_time`, `scheduled_restart_interval_hours`, `scheduled_restart_exclude` (3-필드). UI도 동일 스키마 |
| **제안 수정** | §2.5 탭 6 항목을 "자동 재시작: 기준 시각(HH:MM) + 반복 주기(시간) + 제외 시간대(HH:MM-HH:MM, 쉼표 구분, 자정 넘김 지원) + 활성화 체크박스" 로 재작성 |
| **태그** | 명백 |

### C-2. §11 예약 재시작 전체가 구 방식  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §11 예약 재시작 (441~444행) |
| **문서 내용** | "지정 시각에 프로그램 자동 재시작 (최대 2개 시각 설정) / 10초 주기로 설정 파일 직접 확인 / 같은 슬롯 중복 실행 방지 / 프로그램 비정상 재시작 시 텔레그램 알림 발송 (v2 신규 추가 예정)" |
| **실제 코드** | `main.py:164-227`: 기준 시각 + 주기(시간) 계산 → 제외 시간대 필터 → 30초 주기 `QTimer` → 날짜+시각(YYYY-MM-DD HH:MM) 조합 중복 방지. 비정상 재시작 텔레그램은 이미 구현됨(`main.py:248-250`, `_send_system_telegram_main`) |
| **제안 수정** | §11 전체를 다음과 같이 재작성: "지정 기준 시각에서 N시간 주기로 자동 재시작 / 제외 시간대 지원(HH:MM-HH:MM, 쉼표 구분, 자정 넘김 허용) / 30초 주기 확인 / 날짜+시각 조합으로 중복 실행 방지 / 프로그램 비정상 종료 감지 시 `[SYSTEM]` 텔레그램 알림 발송". "v2 신규 추가 예정" 표현 삭제 |
| **태그** | 명백 |

### C-3. §12 System 설정표가 구 스키마  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §12 System 표 (557~561행) |
| **문서 내용** | `scheduled_restart_enabled`(true), `scheduled_restart_time`("08:56"), `scheduled_restart_time_2`("") |
| **실제 코드** | `default_config.json:106-111` — `scheduled_restart_enabled`(**false**), `scheduled_restart_base_time`("03:00"), `scheduled_restart_interval_hours`(24), `scheduled_restart_exclude`("") |
| **제안 수정** | System 표를 현 스키마 4개 키로 교체. `_enabled` 기본값도 false로 수정 |
| **태그** | 명백 |

### C-4. `notify_system` 키 누락  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §12 Telegram 표 (499~510행) |
| **문서 내용** | `notify_signoff`까지 기재, `notify_system` 없음 |
| **실제 코드** | `default_config.json:59` — `notify_system: true`. `watchdog_process.py:68`·`main.py:46`에서 해당 키 체크하여 `[SYSTEM]` 텔레그램 발송 여부 판정 |
| **제안 수정** | Telegram 표 마지막에 `notify_system \| true \| - \| -` 행 추가. §2.5 탭 6의 "시스템 알림" 섹션 설명 옆에 "`notify_system` 설정 키" 명시 권장 |
| **태그** | 명백 |

### C-5. §13 "v2 추가/변경 사항" vs §11 "추가 예정" 자기모순  *(명백, C-2와 연동)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §11 (444행) vs §13 (567행) |
| **문서 내용** | §11은 "v2 신규 추가 예정", §13은 "프로그램 비정상 재시작 텔레그램 알림 (신규, `[SYSTEM]` prefix로 감지 알림과 분리)"로 이미 완료된 기능으로 기재 |
| **실제 코드** | 구현 완료 (`main.py:248-250`, `watchdog_process.py:_send_system_telegram`) |
| **제안 수정** | §11에서 "(v2 신규 추가 예정)" 문구 삭제. C-2 재작성과 함께 반영 |
| **태그** | 명백 |

### C-6. 설정 키명 `still_block_threshold` vs `still_changed_ratio`  *(애매)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §3.2 스틸 감지(196, 205행), §12 Detection 표(460행) |
| **문서 내용** | 설정 키·표 모두 `still_block_threshold` |
| **실제 코드** | 설정 파일 키: `still_changed_ratio` (`default_config.json:11`, `config_manager.DEFAULT_CONFIG`). UI·저장 경로도 `still_changed_ratio` 사용. 단 **Detector 내부 속성명**은 `still_block_threshold` 로 유지되어 로직·로그에서 쓰임 |
| **제안 수정** | 두 갈래 중 택일 필요: **(a)** 문서를 `still_changed_ratio`로 통일 (설정 스키마 = 외부 계약 우선, 내부 속성은 구현 디테일) — 권장. **(b)** 코드의 설정 키를 `still_block_threshold`로 되돌림 (Plan #2 범위) |
| **태그** | 애매 (사용자 판단 요청) |

### C-7. `audio_tone_*` 3개 키 문서화 누락  *(애매)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | §3 감지 기능, §12 Detection 표 (451~476행) |
| **문서 내용** | `audio_tone_*` 관련 기술 전무 |
| **실제 코드** | `default_config.json:27-29` — `audio_tone_std_threshold`(3.0), `audio_tone_duration`(60.0), `audio_tone_min_level`(5.0). `utils/config_manager.py` DEFAULT_CONFIG 36~38에도 존재 |
| **제안 수정** | 기능이 실제 감지 로직에서 사용되는지 확인 필요. **사용 중**이면 §3.x 새 하위 섹션 "오디오 톤 감지" + §12 Detection 표 3행 추가. **미사용·예약**이면 문서는 그대로 두고 설정 키에 코드 주석으로 "향후 확장용" 명시 권장 |
| **태그** | 애매 (실제 사용 여부 확인 후 결정 — Plan #2와 연관) |

---

## D. 진행표 (`PROGRESS.md`)

### D-1. "마지막 업데이트" 헤더 stale  *(명백)*

| 항목 | 내용 |
|---|---|
| **문서 위치** | `PROGRESS.md:3` |
| **문서 내용** | "> 마지막 업데이트: 2026-04-19" |
| **실제 상태** | 이후 커밋 4건: `e2704a1` (4-19 QTextEdit/SHM), `68b2a38` (4-19 paintEvent 원복), `e0f30ca` (cmd_event 포트전환 개선), `e9c2093` (포트전환 프레임 스틸 수정). 최신 커밋 날짜는 오늘(2026-04-23) 또는 직전 1-2일 |
| **제안 수정** | "> 마지막 업데이트: 2026-04-23" 로 갱신. "현재 단계: Phase 5 진행 중 (코딩 완료 / 실기 테스트 대기 중)" 텍스트는 상태와 일치 → 유지 |
| **태그** | 명백 |

**주의**: Phase 5의 미체크 항목 3건(회귀 테스트·Chaos 테스트·24h 연속)은 실제로도 미완료 상태 → 체크박스는 유지. 추가 드리프트 없음.

---

## E. 루트 지침 (`CLAUDE.md`)

전반 재독 결과 **사실 오류 없음**. 다음은 참고 기록(드리프트 아님):

- `CLAUDE.md`는 현재 345행 → 자체 규칙 "200줄 초과 시 하위 CLAUDE.md 분리" 위반 상태. 하지만 이는 **규칙 준수 문제**로 Plan #2 범위.
- `§아키텍처 — 프로세스 구조` 다이어그램의 스레드 목록은 `processes/detection_process.py`에서 spawn하는 VideoCaptureWorker / AudioMonitorWorker / SignoffManager / AutoRecorder / TelegramWorker / HeartbeatWriter와 1:1 일치.
- `§재spawn 복원 시퀀스`의 재주입 항목(ApplyConfig/SetDetectionEnabled/SetVolume/SetMute/SetSignoffState×2/UpdateROIs)은 `ui/main_window.py:_reinject_runtime_state`와 일치.

---

## 반영 방침 (Step 4 착수 전 확인 사항)

1. **명백 10건**은 일괄 승인 요청.
2. **애매 3건** 개별 판단 필요:
   - **A-1**: `requirements.txt` 생성 vs. 현 상태 유지
   - **C-6**: 문서를 `still_changed_ratio`로 맞춤 vs. 코드를 `still_block_threshold`로 되돌림(Plan #2로 넘김)
   - **C-7**: `audio_tone_*` 기능을 문서에 기술 vs. 설정만 남기고 문서 미기록
3. Step 4 반영은 승인 범위 내에서만 수행. PROGRESS.md는 체크박스 + 헤더 텍스트 둘 다 검토.
4. 본 보고서 하단에 반영 후 "반영 완료 (커밋 대상 목록)" 섹션을 추가 기록.

---

## 반영 완료 (커밋 대상 목록)

**반영 일시**: 2026-04-23
**승인 범위**: 명백 10건 일괄 승인 + 애매 3건 추천안대로 처리

### 편집된 파일

**1. `docs_ipc_spec.md`** (IPC 스펙, 4건)
- B-1: §0 토폴로지에 `cmd_event` 행 추가 + §3.1 기동 시퀀스 4단계에 `cmd_event` 생성 포함
- B-2: §2.1 result_queue 표에 `DetectionCrashed` 행 추가 + §2.3 drop 금지 목록에 추가
- B-3: §3.2 step 7 Watchdog join timeout `5s` → `8s`
- B-4: §3.3 step 2 `LogEntry(level='error', ...)` → `DetectionCrashed(dead_pid, reason, stale_sec)`

**2. `docs_기능_레퍼런스.md`** (기능 레퍼런스, 6건)
- C-1: §2.5 탭 6 자동 재시작 설명 — 2-슬롯 → 기준시각+주기+제외시간대 방식
- C-2: §11 전체 재작성 — 구 "최대 2개 시각/10초 주기/슬롯 중복 방지" → 현 "기준시각+주기/30초 주기/날짜+시각 중복 방지"
- C-3: §12 System 설정표 — `scheduled_restart_time`/`_time_2` 삭제, `base_time`/`interval_hours`/`exclude` 3행 추가, `_enabled` 기본값 False로 수정
- C-4: §12 Telegram 표에 `notify_system` 행 추가
- C-5: §11의 "v2 신규 추가 예정" 문구 삭제 (C-2 재작성에 포함)
- C-6: `still_block_threshold` → `still_changed_ratio` 통일 (§3.2 본문 1곳 + §3.2 표 1곳 + §12 표 1곳, 총 3곳)

**3. `PROGRESS.md`**
- D-1: 헤더 "마지막 업데이트: 2026-04-19" → "2026-04-23"

**4. `CLAUDE.md`**
- 편집 없음 (사실 오류 없음 확인)

### 미반영 (추천안대로 보류)

- **A-1**: `requirements.txt` 생성 — Plan #1 비범위로 유지 (별도 결정 사안, 보고서에 상태만 기록)
- **C-7**: `audio_tone_*` 키 — `config_manager`·`default_config.json`에만 존재하고 실제 감지 로직 미사용 → **미사용/예약 키**로 판정, 문서 편집 불필요. 코드 주석 추가는 Plan #2 범위로 이월

### 검증

- `git diff`: Core 4 문서(`docs_ipc_spec.md`, `docs_기능_레퍼런스.md`, `PROGRESS.md`, 본 보고서)에 한정됨
- 편집 대상 외 파일 변경 없음
- 각 편집 위치를 보고서의 "실제 코드" 행과 교차 확인하여 재현 가능
