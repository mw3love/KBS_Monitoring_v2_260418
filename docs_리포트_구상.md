# 일간/주간 리포트 구상 (보류 — 착수 전 백로그)

> **상태: 보류.** 작성 2026-07-13. 코드는 아직 한 줄도 건드리지 않았다.
> AI 해석 계층(`docs_ai_계층_구상.md`)과 **완전히 독립**이다 — 게이트웨이 키 없이도, 설정창 문제와 무관하게 착수 가능하다.

## 1. 무엇을 하려는가

알람 이력을 일/주 단위로 요약해 **텔레그램 발송 + 앱 내 표시**.

## 2. 핵심 원칙 — 이건 AI 기능이 아니다

**리포트의 뼈대는 AI가 아니라 「이벤트 구조화 저장 + 규칙기반 집계」다.**

### ⚠ 숫자는 코드가, 문장만 AI가

"어제 블랙 3회, 스틸 12회, 정파 2회" 같은 집계는 **코드가 정확히 센다.**
LLM에게 로그를 던져 세어달라고 하면 **환각이 곧 오보**가 되고,
방송사고 리포트에서 틀린 숫자는 **없는 리포트보다 나쁘다.**

AI의 자리는 그 다음이다 — 코드가 만든 정확한 집계표를 주고
"이번 주 V7의 스틸이 평소의 4배인데 대부분 새벽 2~4시에 몰려 있음" 같은
**패턴 코멘트 한 문단**만 쓰게 한다. AI가 없어도 리포트는 완결되어야 한다.

### ⚠ 모달 팝업 반대

이 앱은 **24시간 무인 감시 화면**이다. 모달 팝업이 뜨면 그 순간 모니터링 화면을 가리고,
아무도 안 닫으면 계속 가린 채로 남는다. 리포트 하나 보여주자고 감시 화면을 덮는 건 손익이 맞지 않는다.
→ **텔레그램 발송 + 앱 내 전용 탭(또는 로그 항목)**으로, 사용자가 보고 싶을 때 연다.

## 3. 선행 조건 — 이벤트 구조화 저장

현재 알람 이력은 `logs/YYYYMMDD_detection.txt` 등 **텍스트 로그에만** 남는다.
구조화된 이벤트 저장소가 없어서, 리포트를 만들려면 로그를 정규식으로 긁어야 하고
**로그 포맷이 한 번 바뀌면 리포트가 조용히 깨진다.**

→ `data/events.jsonl` 같은 **append-only 이벤트 저장**이 먼저다.
담을 것(초안): 발생시각 / 알람종류(블랙·스틸·오디오·무음·정파) / 채널 라벨 / 매체명 /
지속시간 / 복구시각 / 정파 억제 여부 / 녹화 파일 경로.

**이건 리포트와 별개로 그 자체가 값어치가 있다** — 장기 통계, "지난달 V3 사고 몇 번이었지?" 조회,
감지 파라미터 튜닝의 근거가 모두 여기서 나온다.

## 4. 착수 순서 (쪼갤 것)

| 단계 | 내용 | AI 필요? |
|---|---|---|
| ⓐ | 이벤트 구조화 저장 (`data/events.jsonl` append + 보존기간 정리) | 불필요 |
| ⓑ | 규칙기반 집계 리포트 생성 + 텔레그램 발송 (일간/주간) | 불필요 |
| ⓒ | 앱 내 리포트 표시 (전용 탭 — **팝업 아님**) | 불필요 |
| ⓓ | (선택) 집계표를 AI에 넘겨 특이사항 코멘트 한 문단 | 필요 |

**ⓐ·ⓑ만으로 실용적으로 완결된다.** ⓓ는 있으면 좋은 것이지 전제가 아니다.

## 5. 미결정 사항

| 항목 | 선택지 / 메모 |
|---|---|
| 저장 포맷 | `events.jsonl`(단순·append 안전) vs SQLite(조회 유연) |
| 저장 주체 | Detection 프로세스가 직접 쓸지, `result_queue`를 받는 UI가 쓸지 — **Detection 크래시 시에도 남아야 하므로 Detection 쪽이 유력** |
| 발송 시각 | 일간 몇 시 / 주간 무슨 요일 몇 시 (정파 시간대와 겹치지 않게) |
| 보존 기간 | 이벤트 로그 몇 개월 유지할지 (자동녹화의 `max_keep_days`와 별개) |
| 리포트 내용 | 알람 건수·채널별 순위·최장 지속·정파 이력·복구 실패 건 등 무엇을 담을지 |
| 앱 표시 위치 | 전용 탭 vs 로그 위젯 항목 |

## 6. 구현 제약 (`detection/CLAUDE.md`)

- Detection 프로세스: **PySide6 임포트 금지**, 200ms 메인 루프 **블로킹 금지**
  → 파일 쓰기는 메인 루프에서 직접 하지 말고 워커 스레드/버퍼 경유 검토
- 설정 추가 시 **3파일 동시 갱신**: `config/default_config.json` / `utils/config_manager.py`의 `DEFAULT_CONFIG` / `config/kbs_config.json`
- 로그 파일 동시 쓰기 충돌 회피 원칙(프로세스별 분리)을 이벤트 파일에도 동일하게 적용할 것
