# AI 계층 구상 (보류 — 착수 전 백로그)

> **상태: 보류.** 설정 다이얼로그 문제(`fix/260526_설정다이얼로그_TypeError_재현불가.md`)가 해결된 뒤 착수한다.
> 작성 2026-07-13. 코드는 아직 한 줄도 건드리지 않았다.

## 1. 무엇을 하려는가

KAIROS Chat API Gateway(`https://docs.factchat.kr/docs/kbs/api-gateway/`)를 붙여
**알람이 이미 울린 뒤에 "이게 뭐로 보이는지" 해석만 얹는 2차 계층**을 만든다.

- 규칙기반 detector(200ms 루프)는 **그대로 둔다.** AI는 감지 판정에 관여하지 않는다.
- **AI는 알람을 억제하지 않는다.** "테스트영상 추정"이라고 해도 알람은 그대로 뜨고, 끄는 건 사람이다.
  (AI 판정으로 알람을 자동 해제하면 그 순간 AI 오판 = 방송사고 놓침이 된다.)
- **절대 원칙: AI 호출을 텔레그램 알람 발송 경로에 직렬로 걸지 않는다.**
  알람은 지금처럼 즉시 나가고, AI 해석은 후속으로 붙는다. 게이트웨이 장애가 알람 지연·누락이 되면 안 된다.
- 키 미발급/미설정 시 **기능 비활성 폴백** — 앱은 지금과 동일하게 동작해야 한다.

### 핵심 아이디어: 단일 프레임이 아니라 다중 프레임

"본사 테스트영상 vs 진짜 정지화면"은 **정지 사진 1장으로는 원리적으로 구별 불가**하다.
구별 정보가 프레임 *사이*에 있기 때문이다(시각 표시·자막·로고 애니메이션의 변화 여부).
→ 사고 직전 10초에서 **3~5장을 시간 간격으로 뽑아 다중 이미지로** 보낸다.

### 사용자에게 보이는 모습

알람은 지금과 동일하게 즉시 도착하고, 3~8초 뒤 해석이 따라붙는다.

```
[KBS On-Air Monitoring 🚨 알림]     ← 기존 그대로 (0초)
📡 채널: V3 (KBS2)
⚠ 감지: 스틸

🤖 AI 해석 (V3 · 스틸)              ← 후속 (3~8초 뒤)
판정: 테스트영상 추정 (확신도 중간)
근거: 10초간 컬러바가 고정돼 있으나 우하단 시각 표시가 계속 변함.
      신호 상실이 아니라 본사 테스트영상 송출로 보임.
```

앱 로그 위젯에도 한 줄(`03:12:11 [AI] V3 스틸 → 테스트영상 추정(중간)`).

## 2. 코드 검토에서 드러난 사실 (⚠ 착수 시 반드시 반영)

초기 구상은 "`detection/auto_recorder.py`의 순환버퍼에서 프레임을 뽑는다"였으나,
**실제 코드에서 그대로 작동하지 않는다.** 확인된 두 가지:

**① 순환버퍼는 자동녹화가 켜져 있을 때만 채워진다**
`auto_recorder.py`의 `push_frame()` 첫 줄이 `if not self._enabled: return`.
자동녹화를 끄면 버퍼가 비어 AI 기능이 **에러 없이 조용히 무력화**된다.

**② 버퍼 프레임은 "전체 멀티뷰 화면을 960×540으로 줄인" JPEG다 — 채널 단위가 아니다**
ROI는 한 캡처 화면 안의 좌표 타일(`core/roi_manager.py`의 `x,y,w,h`)이고,
버퍼는 `configure()`의 `output_width/height`로 리사이즈 후 저장한다.
- 통째로 보내면 → AI가 16개 타일 중 **어느 채널이 문제인지 모른다**
  (현재 텔레그램 스냅샷도 ROI 크롭이 아니라 전체 화면이다 — `detection_process.py`의 `snap`)
- 크롭하면 → 16분할 기준 채널당 **240×135**밖에 안 남아 미세 판독에 해상도가 부족하다

**→ 결론: AI용 프레임 소스는 auto_recorder 버퍼에 얹지 말고, 원본 해상도 프레임에서
ROI를 크롭해 담는 독립 링버퍼를 신설한다** (자동녹화 on/off와 무관하게 동작).

## 3. 기존 규칙기반 해법과의 경계 (중복 주의)

"본사 테스트영상 추정"은 **이미 규칙기반으로 부분 해결돼 있다** —
`detection/signoff_flap.py` + 텔레그램 문구 "정파 진입/복귀가 반복 감지됨(본사 테스트영상 추정)".

착수 전에 정할 것: AI가 기존 플래핑 판정에 **무엇을 더하는지**, 둘이 상반된 결론을 내면 **무엇을 믿는지**.
목표가 "정파 오탐 줄이기"뿐이라면 AI를 붙이기 전에 **signoff_flap 튜닝으로 해결되는지부터** 재는 게 비용·복잡도 면에서 먼저다.

## 4. 미결정 사항

| 항목 | 선택지 | 메모 |
|---|---|---|
| 전달 경로 | 별도 메시지 / 원 알람에 답글 | 답글은 `message_id` 보관 필요 — TelegramWorker가 지금 버림(`return resp.status_code == 200`). **알람의 생명선을 건드리는 변경** |
| 적용 범위 | 블랙·스틸·정파만 | 무음·오디오레벨은 이미지로 판정 불가 |
| UI 표시 깊이 | 로그 한 줄 / 알람 패널 배지 | |
| 호출 폭주·비용 | 채널별 쿨다운·동시호출 상한·일일 상한 | 16채널 동시 알람 대비. TelegramWorker의 `_last_sent` 쿨다운 패턴 재사용 가능 |

**1차 착수 권장 형태:** 별도 메시지 + 로그 한 줄로 **가장 얇게** 붙이고,
실제 쓸모가 확인된 뒤 답글 연결·UI 배지를 얹는다.

## 5. 구현 제약 (`detection/CLAUDE.md`)

- Detection 프로세스: **PySide6 임포트 금지**, 200ms 메인 루프 **블로킹 금지**
- `telegram_worker.py`의 **daemon thread + queue 패턴을 그대로 재사용**
  (`ensure_worker_alive`, 재시도 백오프, `_emit` 드롭 정책까지)
- 설정 추가 시 **3파일 동시 갱신**: `config/default_config.json` / `utils/config_manager.py`의 `DEFAULT_CONFIG` / `config/kbs_config.json`

## 6. 미확인 사항

- 게이트웨이의 **vision(다중 이미지)·structured output(`json_schema`, `strict:true`) 지원 여부**는
  개요 문서에서 확인되지 않았다. 확인된 것은 Anthropic 네이티브 엔드포인트
  `POST /v1/gateway/claude/v1/messages/`의 존재까지.
  → 계획에 **스키마 미지원 시 텍스트 파싱 폴백**을 포함할 것.
- **API 키 미발급** 상태라 실호출 검증 불가.

## 7. 관련 문서

- **일간/주간 리포트 구상**: `docs_리포트_구상.md`
  (이 문서와 **독립** — 리포트의 뼈대는 AI가 아니라 이벤트 구조화 저장 + 규칙기반 집계다.
  게이트웨이 키 없이도, 설정창 문제와 무관하게 먼저 착수 가능.)
